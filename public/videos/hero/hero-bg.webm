# Hero Background Video - WebM Format
# WebM fallback for browsers that support it
# Same content as hero-bg.mp4 but in WebM format
# WebM typically provides better compression and quality
# Recommended resolution: 1920x1080px
